{% extends 'ResponsabilitesBundle::layout.html.twig' %}

{% trans_default_domain 'ResponsabilitesBundle' %}

{% block title %}{{ 'materiel.objet.view_all.title'|trans }} – {{ parent() }}{% endblock %}

{% block resp_body %}
	<div class="row">
		<div class="col-lg-8 col-lg-offset-2">
			<h3>{{ 'materiel.objet.view_all.header'|trans }}</h3>
			{% if is_granted('ROLE_MATERIEL') %}
				<a href="{{ path('responsabilites_materiel_new_objet') }}" class="btn btn-primary">{{ 'materiel.objet.view_all.new'|trans }}</a>
			{% endif %}
			<p><a href="{{ path('responsabilites_materiel_view_all_type') }}">{{ 'materiel.objet.view_all.view_type'|trans }}</a></p>
			<div class="panel panel-default">
				<div class="panel-heading">
					<h3 class="panel-title">{{ 'materiel.objet.view_all.existing'|trans }}</h3>
				</div>
				<div class="list-group">
					{% for objet in objets %}
						<a href="{{ path('responsabilites_materiel_view_type', {objet: objet.id}) }}" class="list-group-item">
							{{ objet.nom }}
						</a>
					{% endfor %}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
